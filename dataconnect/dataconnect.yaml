specVersion: "v1beta"
serviceId: "ot-management"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ot-management-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
